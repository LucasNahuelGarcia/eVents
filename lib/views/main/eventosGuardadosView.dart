import 'package:flutter/material.dart';

class EventosGuardadosView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Column(
      children: <Widget>[
        Text(
          "Elementos Guardados",
          style: TextStyle(fontSize: 45),
          
        )
      ],
    );
  }
}
